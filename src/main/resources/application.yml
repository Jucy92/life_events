spring:
  application:
    name: gift-money-service

  # Database Configuration
  # 로컬 개발: 환경변수 또는 기본값 사용
  # 프로덕션: application-prod.yml에서 오버라이드
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/gift_money_db}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:changeme}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: ${DB_POOL_SIZE:5}
      minimum-idle: ${DB_POOL_MIN_IDLE:2}
      connection-timeout: 30000
      idle-timeout: 300000        # 5분 (기존 10분에서 단축)
      max-lifetime: 900000         # 15분 (기존 30분에서 단축)
      leak-detection-threshold: 60000  # 1분 이상 유지되면 경고
      data-source-properties:
        prepareThreshold: 0

  jpa:
    hibernate:
      ddl-auto: update  # 로컬 개발용 (프로덕션에서는 validate로 오버라이드)
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  # Email Configuration (SMTP)
  # 🔒 보안: 이메일 자격증명은 반드시 환경변수로 설정
  # Gmail 사용 시: 2단계 인증 활성화 후 앱 비밀번호 생성 필요
  # https://myaccount.google.com/apppasswords
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

# Application Configuration
app:
  base-url: ${APP_BASE_URL:http://localhost:8080}

# JWT Configuration
# 🔒 보안: JWT 시크릿은 반드시 환경변수로 설정 (기본값 없음)
# 생성 방법: openssl rand -base64 64
jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION:86400000}

# CORS Configuration
cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173,http://localhost:8080}

# Logging (개발 환경용 - 프로덕션에서는 application-prod.yml에서 오버라이드)
logging:
  level:
    com.example.giftmoney: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

# Server
server:
  port: ${PORT:8080}
  error:
    include-stacktrace: on-param
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

# HTTP Encoding
http:
  encoding:
    charset: UTF-8
    enabled: true
