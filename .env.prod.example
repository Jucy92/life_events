# 🔒 프로덕션 환경변수 예시
# 사용 방법:
#   1. 이 파일을 .env.prod로 복사
#   2. 실제 프로덕션 값으로 변경
#   3. 배포 시 환경변수로 설정 또는 파일 업로드
# ⚠️ .env.prod 파일은 절대 Git에 커밋하지 마세요!

# ========================================
# Spring Profile (필수)
# ========================================
SPRING_PROFILES_ACTIVE=prod

# ========================================
# Database Configuration (필수)
# ========================================
DB_URL=jdbc:postgresql://your-supabase-host.pooler.supabase.com:6543/postgres
DB_USERNAME=postgres.your_project_id
DB_PASSWORD=YOUR_PRODUCTION_PASSWORD_HERE
DB_POOL_SIZE=10
DB_POOL_MIN_IDLE=5

# ========================================
# JWT Configuration (필수)
# ========================================
# 🔑 반드시 강력한 랜덤 키로 변경!
# 생성 방법: openssl rand -base64 64
# PowerShell: [Convert]::ToBase64String((1..64 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
JWT_SECRET=YOUR_PRODUCTION_JWT_SECRET_256_BITS_MINIMUM_REQUIRED

# ========================================
# Email Configuration (필수)
# ========================================
# 📧 Gmail: 2단계 인증 후 앱 비밀번호 생성
# https://myaccount.google.com/apppasswords
MAIL_USERNAME=your-production-email@gmail.com
MAIL_PASSWORD=your-app-password-here

# ========================================
# Application Configuration
# ========================================
APP_BASE_URL=https://yourdomain.com

# ========================================
# CORS Configuration
# ========================================
# 🔒 실제 도메인만 허용 (쉼표로 구분)
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ========================================
# Server Configuration
# ========================================
# 클라우드 제공자가 PORT를 자동 할당하는 경우 생략 가능
PORT=8080
