# 🔒 환경변수 설정 예시 파일
# 사용 방법: 이 파일을 .env로 복사한 후 실제 값으로 변경하세요
# ⚠️ .env 파일은 절대 Git에 커밋하지 마세요! (.gitignore에 포함됨)

# ========================================
# Database Configuration (Supabase PostgreSQL)
# ========================================
DB_URL=jdbc:postgresql://your-supabase-host.pooler.supabase.com:6543/postgres
DB_USERNAME=postgres.your_project_id
DB_PASSWORD=YOUR_PRODUCTION_PASSWORD_HERE
DB_POOL_SIZE=10
DB_POOL_MIN_IDLE=5

# ========================================
# JWT Configuration
# ========================================
# 🔑 반드시 강력한 랜덤 키로 변경 (필수!)
# 생성 방법: openssl rand -base64 64
# PowerShell: [Convert]::ToBase64String((1..64 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
JWT_SECRET=YOUR_PRODUCTION_JWT_SECRET_256_BITS_MINIMUM_REQUIRED
JWT_EXPIRATION=86400000

# ========================================
# Email Configuration (SMTP)
# ========================================
# 📧 Gmail 사용 시: 2단계 인증 활성화 후 앱 비밀번호 생성 필요
# https://myaccount.google.com/apppasswords
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password-here

# ========================================
# Application Configuration
# ========================================
APP_BASE_URL=https://yourdomain.com

# ========================================
# Server Configuration
# ========================================
PORT=8080

# ========================================
# CORS Configuration
# ========================================
# 🔒 보안: 프로덕션에서는 실제 도메인만 허용 (쉼표로 구분)
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ========================================
# Logging Configuration (프로덕션)
# ========================================
LOG_LEVEL=INFO
LOG_LEVEL_SQL=WARN
LOG_LEVEL_SQL_BINDER=WARN

# ========================================
# JPA Configuration (프로덕션)
# ========================================
# 🔒 프로덕션: validate 사용 (자동 DDL 비활성화)
JPA_DDL_AUTO=validate
JPA_SHOW_SQL=false

# ========================================
# Error Handling (프로덕션)
# ========================================
# 🔒 프로덕션: 스택트레이스 노출 금지
ERROR_STACKTRACE=never
