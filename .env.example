# 🔒 환경변수 설정 예시 파일
# 실제 사용 시 .env 파일로 복사하여 실제 값으로 변경하세요
# ⚠️ .env 파일은 절대 Git에 커밋하지 마세요!

# Database Configuration (Supabase Production)
DB_URL=jdbc:postgresql://your-supabase-host.supabase.co:5432/postgres
DB_USERNAME=postgres
DB_PASSWORD=YOUR_PRODUCTION_PASSWORD_HERE

# JWT Configuration
# 🔑 반드시 강력한 랜덤 키로 변경: openssl rand -base64 64
JWT_SECRET=YOUR_PRODUCTION_JWT_SECRET_256_BITS_MINIMUM
JWT_EXPIRATION=86400000

# CORS Configuration (실제 도메인만 허용)
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Server Port (클라우드 제공자가 자동 할당하는 경우도 있음)
PORT=8080

# Spring Profile (프로덕션 프로파일 활성화)
SPRING_PROFILES_ACTIVE=prod
